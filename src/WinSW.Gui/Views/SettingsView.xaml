<UserControl
    x:Class="WinSW.Gui.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:WinSW.Gui.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=vm:ShellViewModel}"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="Row" TargetType="Grid">
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>
        <Style x:Key="RowLabel" TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,16,0" />
            <Setter Property="Width" Value="160" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="28,24,28,20" Style="{StaticResource PageRoot}">
        <Grid.RenderTransform>
            <TranslateTransform />
        </Grid.RenderTransform>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="760" HorizontalAlignment="Left">
                <TextBlock Style="{StaticResource PageTitle}" Text="{DynamicResource M.Nav.Settings}" />
                <TextBlock Margin="0,4,0,18" Style="{StaticResource PageSubtitle}" Text="{DynamicResource M.Settings.Subtitle}" />

                <!-- Newer GUI release -->
                <Border Margin="0,0,0,14" Padding="14,10" Background="{DynamicResource AccentSoftBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="8" Visibility="{Binding HasGuiUpdate, Converter={StaticResource BoolToVisibility}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding GuiUpdateText}" />
                        <Button Margin="14,0,0,0" Padding="10,4" Command="{Binding OpenGuiUpdateCommand}" Content="{DynamicResource M.Shell.OpenRelease}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource M.Settings.Appearance}" />
                        <Grid Style="{StaticResource Row}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource RowLabel}" Text="{DynamicResource M.Shell.Language}" />
                                <ComboBox MinWidth="220" DisplayMemberPath="NativeName" ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}" />
                            </StackPanel>
                        </Grid>
                        <Grid Style="{StaticResource Row}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource RowLabel}" Text="{DynamicResource M.Shell.Theme}" />
                                <ComboBox MinWidth="220" DisplayMemberPath="Label" ItemsSource="{Binding Themes}" SelectedItem="{Binding SelectedTheme}" />
                            </StackPanel>
                        </Grid>
                        <CheckBox Content="{DynamicResource M.Settings.CollapseRail}" IsChecked="{Binding IsRailCollapsed}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource M.Settings.Behaviour}" />
                        <CheckBox Margin="0,0,0,8" Content="{DynamicResource M.Shell.MinimizeToTray}" IsChecked="{Binding MinimizeToTray}" />
                        <CheckBox Margin="0,0,0,8" Content="{DynamicResource M.Shell.NotifyOnStop}" IsChecked="{Binding NotifyOnUnexpectedStop}" />
                        <CheckBox Content="{DynamicResource M.Shell.ContextMenu}" IsChecked="{Binding ContextMenuRegistered}" ToolTip="{DynamicResource M.Shell.ContextMenuTip}" />
                        <TextBlock Margin="24,2,0,0" Style="{StaticResource FieldHint}" Text="{DynamicResource M.Shell.ContextMenuTip}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource M.Settings.Shortcuts}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.Resources>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
                                    <Setter Property="Margin" Value="0,0,0,6" />
                                </Style>
                            </Grid.Resources>
                            <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource Mono}" Text="F5" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{DynamicResource S.Rescan}" />
                            <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource Mono}" Text="Ctrl+S" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{DynamicResource S.Save}" />
                            <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource Mono}" Text="Esc" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{DynamicResource S.Cancel}" />
                            <TextBlock Grid.Row="3" Grid.Column="0" Style="{StaticResource Mono}" Text="Ctrl+C" />
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{DynamicResource M.Log.CopyTip}" />
                            <TextBlock Grid.Row="4" Grid.Column="0" Style="{StaticResource Mono}" Text="Ctrl+Wheel" />
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{DynamicResource M.Log.ZoomTip}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource M.Settings.About}" />
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" Text="WinSW GUI " />
                            <TextBlock Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding GuiVersion}" />
                        </StackPanel>
                        <TextBlock Style="{StaticResource PageSubtitle}" Text="{DynamicResource M.Settings.AboutBody}" />
                        <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                            <Button Margin="0,0,8,0" Padding="10,5" Click="OnOpenLink" Tag="https://github.com/Ly-star-bit/winsw-gui" Content="GitHub" />
                            <Button Margin="0,0,8,0" Padding="10,5" Click="OnOpenLink" Tag="https://github.com/winsw/winsw/blob/v3/docs/xml-config-file.md" Content="{DynamicResource M.Settings.WrapperDocs}" />
                            <Button Padding="10,5" Click="OnOpenLink" Tag="https://github.com/Ly-star-bit/winsw-gui/issues" Content="{DynamicResource M.Settings.ReportIssue}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
