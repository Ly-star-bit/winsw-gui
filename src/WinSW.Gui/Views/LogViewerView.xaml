<UserControl
    x:Class="WinSW.Gui.Views.LogViewerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:WinSW.Gui.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=vm:LogViewerViewModel}"
    mc:Ignorable="d">

    <Grid Margin="28,24,28,20" Style="{StaticResource PageRoot}">
        <Grid.RenderTransform>
            <TranslateTransform />
        </Grid.RenderTransform>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{StaticResource PageTitle}" Text="Logs" />
                <StackPanel Margin="0,4,0,0" Orientation="Horizontal">
                    <TextBlock Style="{StaticResource PageSubtitle}" Text="{Binding ServiceName}" />
                    <TextBlock Margin="10,0" Style="{StaticResource PageSubtitle}" Text="·" />
                    <TextBlock Style="{StaticResource Mono}" Text="{Binding LogDirectory}" />
                </StackPanel>
            </StackPanel>
            <Button Grid.Column="1" VerticalAlignment="Center" Padding="12,7" Command="{Binding RescanCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="0,0,8,0" Style="{StaticResource Icon}" Text="&#xE72C;" />
                    <TextBlock Text="Rescan files" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- Toolbar -->
        <Border Grid.Row="1" Margin="0,0,0,12" Padding="12,10" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ComboBox
                    Grid.Column="0"
                    MinWidth="320"
                    HorizontalAlignment="Left"
                    DisplayMemberPath="Caption"
                    ItemsSource="{Binding Files}"
                    SelectedItem="{Binding SelectedFile}" />

                <TextBox
                    Grid.Column="1"
                    Width="220"
                    Margin="12,0"
                    VerticalAlignment="Center"
                    Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                    ToolTip="Show only lines containing this text" />

                <CheckBox Grid.Column="2" Margin="0,0,12,0" VerticalAlignment="Center" Content="Follow" IsChecked="{Binding AutoScroll}" />

                <Button Grid.Column="3" Margin="0,0,8,0" Padding="12,6" Command="{Binding TogglePauseCommand}" Content="{Binding PauseLabel}" />
                <Button Grid.Column="4" Margin="0,0,8,0" Padding="12,6" Command="{Binding ClearCommand}" Content="Clear" />
                <Button Grid.Column="5" Margin="0,0,8,0" Padding="12,6" Command="{Binding OpenExternallyCommand}" Content="Open" ToolTip="Open the file with its default application" />
                <Button Grid.Column="6" Padding="12,6" Command="{Binding RevealCommand}" Content="Reveal" ToolTip="Show the file in Explorer" />
            </Grid>
        </Border>

        <!-- Output -->
        <Border Grid.Row="2" Padding="0" Background="{StaticResource InputBrush}" Style="{StaticResource Card}">
            <ListBox
                x:Name="Output"
                Background="Transparent"
                BorderThickness="0"
                FontFamily="{StaticResource MonoFont}"
                FontSize="12"
                ItemsSource="{Binding Lines}"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="12,1" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="MinHeight" Value="0" />
                        <Setter Property="Focusable" Value="False" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            Foreground="{Binding Converter={StaticResource LogSeverityToBrush}}"
                            Text="{Binding}"
                            TextWrapping="NoWrap" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <TextBlock Grid.Row="3" Margin="0,12,0,0" Style="{StaticResource PageSubtitle}" Text="{Binding StatusMessage}" />
    </Grid>
</UserControl>
